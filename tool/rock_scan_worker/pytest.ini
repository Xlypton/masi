[pytest]
testpaths = tests
markers =
    integration: needs real ffmpeg and/or COLMAP on PATH; skipped when absent
    slow: takes tens of seconds (a genuine reconstruction)
filterwarnings =
    error::DeprecationWarning:rock_scan_worker.*
