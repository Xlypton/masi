import 'ar_segmentation_channel.dart';

/// Native (iOS/Android/desktop) factory: a real [ArSegmentationChannel]
/// backed by the actual `masi/arSegmentation` platform channel.
ArSegmentationChannel createArSegmentationChannel() => ArSegmentationChannel();
